import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    mensagem:{
       fontSize:25,
       margin:10,
       color: 'purple',
    },
    viewmensagem: {
        borderWidth:2,
        borderColor:'violet',
        borderRadius:20,
        marginBotton:10,
        padding: 10,
    }
});

export default styles;